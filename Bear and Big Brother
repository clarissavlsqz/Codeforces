#include <iostream>
#include <string>
using namespace std;

int main(int argc, const char * argv[]) {
  int limakAge, bobAge, year = 0;
    cin >> limakAge;
    cin >> bobAge;
    
    while (limakAge <= bobAge) {
        limakAge = limakAge * 3;
        bobAge = bobAge * 2;
        year++;
    }
    
  cout << year;
  return 0;
  }
